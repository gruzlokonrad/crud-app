export const getAllCategories = ({categories}) => categories

const categoriesReducer = (statePart = [], {type, payload}) => {
  switch (type) {
    default:
      return statePart;
  }
}

export default categoriesReducer